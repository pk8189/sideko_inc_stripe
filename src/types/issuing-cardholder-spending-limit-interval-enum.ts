/**
 * Interval (or event) to which the amount applies.
 */
export type IssuingCardholderSpendingLimitIntervalEnum =
  | "all_time"
  | "daily"
  | "monthly"
  | "per_authorization"
  | "weekly"
  | "yearly";
