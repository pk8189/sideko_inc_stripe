/**
 * If `replacement_for` is specified, this should indicate why that card is being replaced.
 */
export type IssuingCardCreateBodyReplacementReasonEnum =
  | "damaged"
  | "expired"
  | "lost"
  | "stolen";
