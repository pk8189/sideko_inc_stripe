/**
 * The entity that requested the exemption, either the acquiring merchant or the Issuing user.
 */
export type IssuingAuthorizationAuthenticationExemptionClaimedByEnum =
  | "acquirer"
  | "issuer";
