/**
 * Interval (or event) to which the amount applies.
 */
export type IssuingCardSpendingLimitIntervalEnum =
  | "all_time"
  | "daily"
  | "monthly"
  | "per_authorization"
  | "weekly"
  | "yearly";
