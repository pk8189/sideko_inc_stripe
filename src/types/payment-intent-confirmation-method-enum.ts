/**
 * Describes whether we can confirm this PaymentIntent automatically, or if it requires customer action to confirm the payment.
 */
export type PaymentIntentConfirmationMethodEnum = "automatic" | "manual";
