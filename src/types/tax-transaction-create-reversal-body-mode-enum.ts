/**
 * If `partial`, the provided line item or shipping cost amounts are reversed. If `full`, the original transaction is fully reversed.
 */
export type TaxTransactionCreateReversalBodyModeEnum = "full" | "partial";
